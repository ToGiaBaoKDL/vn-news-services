version: 1
owner: vn-news-services
platforms:
  - linux/amd64
  - linux/arm64

change_paths:
  - .github/workflows/publish-images.yaml
  - .dockerignore
  - images.yaml
  - pyproject.toml
  - services/common/**
  - uv.lock

images:
  service_feed_ingestor:
    image_repository: vn-news-feed-ingestor
    change_paths:
      - services/feed_ingestor/**
    build:
      context: vn-news-services
      dockerfile: services/feed_ingestor/Dockerfile
      additional_contexts:
        vn-news-platform-lib: vn-news-platform-lib

  service_article_fetcher:
    image_repository: vn-news-article-fetcher
    change_paths:
      - services/article_fetcher/**
    build:
      context: vn-news-services
      dockerfile: services/article_fetcher/Dockerfile
      additional_contexts:
        vn-news-platform-lib: vn-news-platform-lib

  service_article_extractor:
    image_repository: vn-news-article-extractor
    change_paths:
      - services/article_extractor/**
    build:
      context: vn-news-services
      dockerfile: services/article_extractor/Dockerfile
      additional_contexts:
        vn-news-platform-lib: vn-news-platform-lib

  service_pipeline_metrics:
    image_repository: vn-news-pipeline-metrics
    change_paths:
      - services/pipeline_metrics/**
    build:
      context: vn-news-services
      dockerfile: services/pipeline_metrics/Dockerfile
      additional_contexts:
        vn-news-platform-lib: vn-news-platform-lib

  service_dlq_operator:
    image_repository: vn-news-dlq-operator
    change_paths:
      - services/dlq_operator/**
    build:
      context: vn-news-services
      dockerfile: services/dlq_operator/Dockerfile
      additional_contexts:
        vn-news-platform-lib: vn-news-platform-lib
