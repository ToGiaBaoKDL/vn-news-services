[project]
name = "news-article-extractor"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "beautifulsoup4>=4.13",
  "news-platform-lib",
  "news-service-common",
]

[tool.uv.sources]
news-platform-lib = { path = "../../../vn-news-platform-lib", editable = true }
news-service-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/news_article_extractor"]
